#Create a program that generates a pattern of numbers in the shape of a pyramid.
def number_pyramid(rows):
    for i in range(1, rows + 1):
        
        print(" " * (rows - i), end="")
        for j in range(1, i + 1):
            print(j, end=" ")

        print()  
number_pyramid(5)
